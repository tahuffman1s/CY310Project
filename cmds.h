// Cy310 project
// Cmd Class

#include <iostream>
#include <vector>
#inlcude "strm.h"


class cmds {
private:

public:
        bool check(std::string);
        void help();
        void login(std::vector<std::string>);
        void reg(std::vector<std::string>);
        void ftplock(std::string)
};
