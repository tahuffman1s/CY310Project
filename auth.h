// Cy310 project
// Cmd Class

#include <iostream>
#include <vector>

class auth {
private:

public:
bool check(std::string);
void login(std::vector<std::string>, int &priv);
void reg(std::vector<std::string>, int &priv);
};
